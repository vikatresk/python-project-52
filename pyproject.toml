[project]
name = "hexlet-code"
version = "0.1.0"
description = "Task manager"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "django>=5.1.6",
    "django-bootstrap5>=25.1",
    "gunicorn>=23.0.0",
    "python-dotenv>=1.1.0",
]
